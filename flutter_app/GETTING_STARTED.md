# Flutter 项目快速开始指南

## 📦 安装 Flutter

如果还没有安装 Flutter，请先安装：

1. 访问 [Flutter 官网](https://flutter.dev/docs/get-started/install)
2. 下载并安装 Flutter SDK
3. 运行 `flutter doctor` 检查环境

## 🚀 快速开始

### 1. 进入项目目录

```bash
cd flutter_app
```

### 2. 安装依赖

```bash
flutter pub get
```

### 3. 运行项目

```bash
# Web 平台（推荐用于快速测试）
flutter run -d chrome

# Android
flutter run -d android

# iOS
flutter run -d ios

# Windows
flutter run -d windows

# macOS
flutter run -d macos

# Linux
flutter run -d linux
```

## 🔧 配置后端地址

1. 启动应用后，在登录页面会显示当前后端地址
2. 点击"配置"按钮
3. 输入后端服务器地址（例如: `http://localhost:9999`）
4. 点击"保存"
5. 配置会自动保存，下次启动时自动加载

## 📱 功能说明

### 登录功能

- 支持用户名和密码登录
- 自动保存登录状态（Cookie）
- 登录成功后跳转到主页（待实现）

### 后端地址配置

- 支持动态配置后端服务器地址
- 配置保存在本地，持久化存储
- 支持 HTTP 和 HTTPS 协议

## 🏗️ 项目结构

```
flutter_app/
├── lib/
│   ├── core/              # 核心功能
│   │   ├── config/        # 配置管理（后端地址等）
│   │   ├── models/        # 数据模型
│   │   ├── services/      # API 服务
│   │   └── utils/         # 工具类
│   ├── features/          # 功能模块
│   │   └── auth/         # 认证模块
│   │       ├── pages/    # 登录页面
│   │       └── services/ # 认证服务
│   ├── shared/           # 共享组件
│   │   └── widgets/     # 通用组件
│   └── main.dart        # 应用入口
├── assets/              # 资源文件
├── pubspec.yaml         # 依赖配置
└── README.md           # 项目说明
```

## 🔗 后端 API 对接

应用已配置好与 Next.js 后端的 API 对接：

- **登录 API**: `POST /api/v1/user` (action: login)
- **注册 API**: `POST /api/v1/user` (action: register)
- **Cookie 管理**: 自动处理登录后的 Cookie

## 📝 下一步开发

1. 实现主页功能
2. 实现任务列表功能
3. 实现礼物列表功能
4. 实现留言功能
5. 实现用户信息管理

## 🐛 常见问题

### Flutter 未安装

如果提示 `flutter: 无法识别`，请：
1. 安装 Flutter SDK
2. 将 Flutter 添加到系统 PATH
3. 运行 `flutter doctor` 检查

### 依赖安装失败

```bash
# 清理缓存
flutter clean
flutter pub get
```

### 后端连接失败

1. 确保后端服务器正在运行
2. 检查后端地址配置是否正确
3. 检查网络连接
