# RomanceHub Flutter 客户端

全端通用的 Flutter 应用，支持 iOS、Android、Web、Windows、macOS、Linux。

## 🚀 功能特性

- ✅ **多平台支持** - iOS、Android、Web、Desktop
- ✅ **后端地址配置** - 登录页面可配置后端服务器地址
- ✅ **用户认证** - 登录、注册功能
- ✅ **任务管理** - 任务列表、详情、发布、状态管理
- ✅ **礼物系统** - 礼物列表、添加、兑换
- ✅ **留言功能** - 我的留言、TA的留言、发布留言
- ✅ **收藏功能** - 任务/礼物/留言收藏管理
- ✅ **用户信息** - 个人信息、关联者信息查看
- ✅ **类型安全** - 完整的 Dart 类型系统
- ✅ **现代化 UI** - Material Design 3，简洁明了的设计风格

## 📋 前置要求

- Flutter SDK >= 3.0.0
- Dart SDK >= 3.0.0

## 🛠️ 安装和运行

### 1. 安装依赖

```bash
cd flutter_app
flutter pub get
```

### 2. 运行项目

```bash
# 运行在 Web
flutter run -d chrome

# 运行在 Android
flutter run -d android

# 运行在 iOS
flutter run -d ios

# 运行在 Windows
flutter run -d windows

# 运行在 macOS
flutter run -d macos

# 运行在 Linux
flutter run -d linux
```

## 📱 使用说明

### 配置后端地址

1. 打开应用后，在登录页面点击"配置"按钮
2. 输入后端服务器地址（例如: `http://localhost:9999`）
3. 点击"保存"
4. 配置会自动保存，下次启动时会自动加载

### 登录

1. 确保后端服务器正在运行
2. 在登录页面输入用户名和密码
3. 点击"登录"按钮

## 🏗️ 项目结构

```
lib/
├── core/                    # 核心功能
│   ├── config/             # 配置管理
│   ├── constants/          # 常量定义
│   ├── models/             # 数据模型
│   ├── routes/             # 路由配置
│   ├── services/           # 服务层
│   └── utils/              # 工具类
├── features/               # 功能模块
│   ├── auth/              # 认证模块
│   ├── home/              # 主页
│   ├── task/              # 任务模块
│   ├── gift/              # 礼物模块
│   ├── whisper/           # 留言模块
│   ├── favourite/         # 收藏模块
│   └── user/              # 用户模块
└── shared/                # 共享组件
    └── widgets/           # 通用组件
```

## 🔧 开发

### 代码生成

```bash
# 生成 JSON 序列化代码
flutter pub run build_runner build --delete-conflicting-outputs
```

### 类型检查

```bash
flutter analyze
```

## 📦 依赖说明

- **provider** - 状态管理
- **dio** - HTTP 客户端
- **shared_preferences** - 本地存储
- **flutter_secure_storage** - 安全存储
- **go_router** - 路由管理
- **image_picker** - 图片选择
- **logger** - 日志工具
- **intl** - 国际化支持

## 🔗 后端 API

应用需要连接到运行在 `http://localhost:9999` 的 Next.js 后端服务器。

主要 API 端点：
- `POST /api/v1/user` - 用户登录/注册
- `GET /api/v1/task` - 任务列表
- `GET /api/v1/gift` - 礼物列表
- `GET /api/v1/whisper` - 留言列表

## 📄 许可证

MIT License
