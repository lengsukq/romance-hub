# 锦书

两心相知，一事一诺。全端通用应用，支持 iOS、Android、Web、Windows、macOS、Linux。

## 🚀 功能特性

- ✅ **多平台支持** - iOS、Android、Web、Desktop
- ✅ **云阁配置** - 登录页可配置后端地址
- ✅ **用户认证** - 登录、二人同契（双账号注册）
- ✅ **心诺** - 心诺一览、详情、立一诺、状态管理
- ✅ **赠礼** - 赠礼一览、添心意、兑换
- ✅ **私语** - 我的私语、TA的私语、写私语
- ✅ **藏心** - 心诺/赠礼/私语收藏
- ✅ **吾心** - 吾之信息、良人信息
- ✅ **类型安全** - 完整的 Dart 类型系统
- ✅ **现代化 UI** - Material Design 3，简洁明了

## 📋 前置要求

- Flutter SDK >= 3.0.0
- Dart SDK >= 3.0.0

## 🛠️ 安装和运行

### 1. 安装依赖

```bash
cd flutter_app
flutter pub get
```

### 2. 运行项目

```bash
# 运行在 Web
flutter run -d chrome

# 运行在 Android
flutter run -d android

# 运行在 iOS
flutter run -d ios

# 运行在 Windows
flutter run -d windows

# 运行在 macOS
flutter run -d macos

# 运行在 Linux
flutter run -d linux
```

## 📱 使用说明

### 配置云阁

1. 打开应用后，在登录页点击「配置」
2. 输入云阁地址（如默认 `https://r-d.lengsu.top/`），或点击「复为默认云阁」
3. 点击「保存」，下次启动会自动加载

### 登录

1. 确保云阁（后端）已运行
2. 输入用户名与密码，点击「登入」

### 打包后无法连接云阁？

本地调试（`flutter run`）能连上云阁，但打正式包（如 `flutter build apk`）后连不上，常见原因：

- **正式包首次安装没有保存过云阁地址**，会使用默认地址 `https://r-d.lengsu.top/`。若你的后端是自建或内网（如 `http://192.168.x.x:3000`），需要在安装后的登录页重新配置一次。
- **网络环境不同**：真机与电脑不在同一局域网、或公司/学校网络限制等，导致无法访问你本机的云阁。

**排查方式**：在登录页**连点「锦书」上方爱心图标 5 次**，会打开调试面板，可查看当前构建模式（Debug/Release）、云阁地址、并直接「配置云阁」和「测试连接」。

## 🏗️ 项目结构

```
lib/
├── core/                    # 核心功能
│   ├── config/             # 配置管理
│   ├── constants/          # 常量定义
│   ├── models/             # 数据模型
│   ├── routes/             # 路由配置
│   ├── services/           # 服务层
│   └── utils/              # 工具类
├── features/               # 功能模块
│   ├── auth/              # 认证模块
│   ├── home/              # 主页
│   ├── task/              # 任务模块
│   ├── gift/              # 礼物模块
│   ├── whisper/           # 留言模块
│   ├── favourite/         # 收藏模块
│   └── user/              # 用户模块
└── shared/                # 共享组件
    └── widgets/           # 通用组件
```

## 🔧 开发

### 代码生成

```bash
# 生成 JSON 序列化代码
flutter pub run build_runner build --delete-conflicting-outputs
```

### 类型检查

```bash
flutter analyze
```

## 📦 依赖说明

- **provider** - 状态管理
- **dio** - HTTP 客户端
- **shared_preferences** - 本地存储
- **flutter_secure_storage** - 安全存储
- **go_router** - 路由管理
- **image_picker** - 图片选择
- **logger** - 日志工具
- **intl** - 国际化支持

## 🔗 云阁 API

应用需连接云阁（后端），默认地址：`https://r-d.lengsu.top/`，可在登录页配置。

主要 API 端点：
- `POST /api/v1/user` - 登录/二人同契
- `GET /api/v1/task` - 心诺列表
- `GET /api/v1/gift` - 赠礼列表
- `GET /api/v1/whisper` - 私语列表

## 📄 许可证

MIT License
